#include <iostream>
#include <vector>

class Solution {
public:
    int numOfUnplacedFruits(std::vector<int>& fruits, std::vector<int>& baskets) {
	    int totalFruits = fruits.size();
	    for (int i = 0; fruits[i]; i++) {

	    }
    }
};

int main(void) {
	Solution sol;
	std::vector<int> fruits = {3, 3, 3};
	std::vector<int> baskets = {4, 4, 4};
	printf("%d", sol.numOfUnplacedFruits(fruits, baskets));
}
